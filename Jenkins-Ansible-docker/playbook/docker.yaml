---
- name: create docker image
  hosts: dockerserver
  vars:
      image_name: "finexo"
  tasks:
    - name: Build an image
      docker_image:
        name: "{{ image_name }}"
        build:
          path: "{{ WORKSPACE }}/Jenkins-Ansible-docker/finexo/"
        source: build
